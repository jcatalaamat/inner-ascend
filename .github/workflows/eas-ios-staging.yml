name: EAS iOS Staging (Quick)

on:
  workflow_dispatch:

jobs:
  ios-staging:
    name: iOS Staging Build & Submit
    runs-on: ubuntu-latest
    steps:
      - name: Check for EXPO_TOKEN
        run: |
          if [ -z "${{ secrets.EXPO_TOKEN }}" ]; then
            echo "You must provide an EXPO_TOKEN secret linked to this project's Expo account in this repo's secrets."
            exit 1
          fi

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: yarn

      - name: Setup Expo and EAS
        uses: expo/expo-github-action@v8
        with:
          expo-version: latest
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}
          expo-cli-version: latest

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Trigger iOS Staging Build
        run: |
          cd apps/expo
          echo "🚀 Triggering iOS staging build..."
          eas build --platform ios --profile staging --non-interactive --no-wait
          echo "✅ iOS staging build triggered"
          echo "⏱️  Build will take ~15-25 minutes to complete"
          echo "📋 You can manually submit later when build is ready"

      - name: Status
        run: |
          echo "📋 iOS staging build triggered"
          echo "🔗 Check build status at: https://expo.dev/accounts/inner-ascend-expo/projects/mazunte-connect/builds"
          echo "⏱️  Build: ~15-25 min"
          echo "📱 When build is ready, use EAS Full Production Build to submit"
